const PostTitle = ({children}) =>
    <h1 className="max-w-5xl mx-auto text-3xl md:text-7xl lg:text-7xl font-bold tracking-tighter leading-tight
        md:leading-none mb-12 text-center md:text-left font-serif">
      {children}
    </h1>

export default PostTitle
